package beto

import org.eclipse.swt.widgets._
import view.BeToFrame

object Main {

   def main(args: Array[String]): Unit = {
    val display = Display.getCurrent
    val window = new BeToFrame(display)
    window.setBlockOnOpen(true)
    window.open
  }

/*  def main(args: Array[String]): Unit = {

    val a: Array[String] = Array[String]("a", "a", "c", "c", "s")
    val b: Array[String] = Array[String]("a", "b", "c")

    a.toSet.diff(b.toSet).foreach(e => println(e))
    println("----------------------")
    b.diff(a).foreach(e => println(e))
    println("%s".format(a.size))
  }*/

}

